\documentclass[12pt]{article}

\usepackage[a4paper, margin=2cm]{geometry}
\usepackage[brazil]{babel}
\usepackage{fontspec}
\usepackage{mathtools}
\usepackage{amssymb}
\usepackage{sectsty}

\subsectionfont{\normalsize}
\setlength{\parindent}{0pt}
\setlength{\parskip}{0.5em}

\title{Atividade de FMC I}
\author{Sérgio Dantas}
\date{\today}

\begin{document}
	\maketitle
	
	\section*{\centering Axiomas dos reais}
	
	\subsection*{13. Dê um exemplo de conjunto não vazio e limitado superiormente que não possui supremo em $\mathbb{Q}$.}
	
	Considere o conjunto $A = \{ x \in \mathbb{Q} \mid x > 0 \land x^2 < 2 \}$.
	
	Note que $1 \in \mathbb{Q}$, $1 > 0$ e $1^2 = 1 < 2$. Logo $1 \in A$ e, consequentemente, $A \neq \emptyset$ ($A$ é um conjunto não vazio).
	
	Precisamos encontrar um racional $b$ tal que $(\forall x \in A)[b \geq x]$. Tomemos, então, $b = 2$. Se $x \in A$, então $x^2 < 2$, pela definição do conjunto. Sabemos que $2^2 = 4$ e, portanto, $x^2 < 2 < 4$. Isso implica que $x < 2$ (pois $x^2 < 2^2$ e $x > 0$). Como $2 \in \mathbb{Q}$, logo o conjunto é limitado superiormente por um racional.
\end{document}
